/**
 * Global jest setup/teardown functions
 */
beforeAll(async () => {
  // do setup here
})
beforeEach(async () => {
  // before each test
})
afterAll(async () => {
  // do teardown here
})
afterEach(async () => {
  // after each
})
